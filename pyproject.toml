[project]
name = "taskfzf-tests"
version = "0.1.0"
requires-python = ">=3.10"

[project.optional-dependencies]
test = ["pytest>=7"]

[tool.pytest.ini_options]
testpaths = ["taskfzf-test.py"]
addopts = "-p no:libtmux"